# Skills

This folder contains generated reusable skills derived from repository capabilities.

Each skill represents a structured capability that can be reused inside larger AI systems. A skill may include inputs, outputs, steps, use cases, patterns, prompt templates, and metadata.

These files are generated by the asset pipeline and are intended to serve as reusable system building blocks.
