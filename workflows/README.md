# Workflows

This folder contains generated workflows derived from skills.

A workflow is a lightweight execution layer that maps a skill into step-based orchestration. Workflows are used by the orchestrator to execute reusable processes in a predictable sequence.

In the current version of the project, workflows are intentionally simple and act as the first execution layer on top of generated skills.
