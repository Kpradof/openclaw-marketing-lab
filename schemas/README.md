# Schemas

This folder contains JSON schemas used to validate generated outputs.

Schemas help enforce consistency across capabilities, workflows, and prompt packs so that generated assets remain structured, predictable, and reusable.

Validation is part of the generation pipeline and acts as a quality gate before assets are saved.
