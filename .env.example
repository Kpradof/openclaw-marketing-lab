# Optional: increases GitHub API rate limits for repository discovery.
GITHUB_TOKEN=ghp_replace_with_your_token

# Required only for live LLM capability extraction.
OPENAI_API_KEY=sk_replace_with_your_key

# Optional: limit the number of repositories converted during generation.
REPO_LIMIT=5
